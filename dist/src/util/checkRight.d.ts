/**
 * Set tooltip left so it doesn't go off the right side of the window
 *
 * @return boolean true, if tooltipLayerStyleLeft is ok.  false, otherwise.
 */
export default function checkRight(targetOffset: any, tooltipLayerStyleLeft: any, tooltipOffset: any, windowSize: any, tooltipLayer: any): boolean;
