/**
 * To change the scroll of `window` after highlighting an element
 *
 * @api private
 */
export default function scrollTo(scrollTo: string, targetElement: HTMLElement, tooltipLayer: HTMLElement): void;
