/**
 * Exit from intro
 *
 * @api private
 * @param {Object} targetElement
 * @param {Boolean} force - Setting to `true` will skip the result of beforeExit callback
 */
export default function exitIntro(targetElement: HTMLElement, force: boolean): Promise<void>;
